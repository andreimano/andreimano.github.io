# Retro Preview

This folder now redirects to `../variant-lab/index.html?theme=default`.

The main homepage and variant lab are static pages. Keep `index.html` and
`variant-lab/index.html` in sync when changing homepage content.
