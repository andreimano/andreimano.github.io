# Retro Preview

This folder now redirects to `../variant-lab/index.html?theme=default`.

The main homepage and variant lab share `_includes/variant_home.html`, so
paper/news/content changes only need to be made once.
